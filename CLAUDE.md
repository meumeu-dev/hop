# Hop Project

## Context
Les fichiers dans `.claude-memory/` contiennent le contexte des sessions précédentes.
Au démarrage, lis `.claude-memory/MEMORY.md` pour l'index, puis charge les fichiers pertinents selon la tâche.

## Règles
- Toujours lire `.claude-memory/MEMORY.md` en début de session
- Repo privé GitHub : meumeu-dev/hop
- Build auto via GitHub Actions sur chaque tag v*
